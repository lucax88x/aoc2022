package dayX

import (
	"strings"
)

func DayX1(str string) int {
	lines := strings.Split(str, "\n")

	sum := 0
	for _, line := range lines {
		print(line)
	}
	return sum
}

func DayX2(str string) int {
	lines := strings.Split(str, "\n")

	sum := 0
	for _, line := range lines {
		print(line)
	}
	return sum

}
